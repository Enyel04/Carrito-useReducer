import React from 'react'
import ReactDOM from 'react-dom/client' //Integrarlo con codigo HTML
import App from './App.js'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')! ).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)


//Colocar la exlamacion indicara que el operador no sera nulo